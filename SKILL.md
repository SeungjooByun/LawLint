---
name: lawlint
description: 개발자가 앱/웹 서비스 코드를 작성하거나 리뷰할 때, 법적으로 확인이 필요한 지점이 코드에 보이면 질문 없이 먼저 사용한다. 예를 들어 생년월일·주민등록번호 등 개인정보 수집 필드, 개인정보처리방침 페이지 유무, 회원가입/탈퇴/파기 로직, 개인정보 저장 시 암호화·접근통제 등 보호조치 여부, 해외 서버/리전에 개인정보 저장, AI API 연동이나 생성형 AI 결과물 노출, 챗봇 대화 로그 재활용, 이미지/음성 업로드 후 학습데이터 활용, 생체인식(얼굴인식) 처리, 채용·대출 심사 등 사람 개입 없는 자동화된 의사결정 로직, 세션 리플레이(Hotjar 등)·광고 추적 픽셀(Meta/TikTok) 삽입, 크롤링/스크래핑, 오픈소스 라이선스(GPL/AGPL) 포함, 출처 불명 이미지 사용, 구독 자동갱신·해지 UX, 인앱결제 강제·외부결제 차단, 결제·PG 연동과 사업자등록·세금 처리 시점, 통신판매업 신고 여부, 마케팅성 SMS/알림톡/이메일 자동 발송, 위치정보(GPS) 수집, 게임 등급분류, 암호화 모듈(SSL/HTTPS/VPN) 포함 소프트웨어의 해외 배포·수출, 이미지·영상 업로드형 커뮤니티/UGC 기능(저작권 신고 처리뿐 아니라 불법촬영물 유통방지 책임자 지정 여부도 별도 확인), 후기·리뷰 비공개·필터링 로직, 인플루언서·제휴 링크의 협찬 표시 여부, 웹 접근성(alt 텍스트 등), 병원/의료 매칭 후 수수료 수취 구조, 비대면 진료, 채용 플랫폼 이력서 폼, 외주 개발 계약, 카드·거래내역 등 금융데이터를 동의 범위 밖으로 결합·분석, 가상자산 전송, 과외·재능·전문가 등 두 당사자를 매칭해주고 수수료를 받는 중개형 결제 로직이 코드나 대화에 등장하면 이 스킬을 사용한다. "법적으로 괜찮나요?"라는 질문에도 쓰지만, 질문이 없어도 위 패턴이 보이면 먼저 사용한다.
---

# LawLint v0.1


이 스킬은 자체적으로 법률 지식을 갖고 있지 않는다. 하는 일은 딱 두 가지다: **(1) 지금 코드에 트리거가 있는지 감지하고, (2) 걸리면 실시간으로 공식 소스를 조회하게 만든다.**

## 절차

1. `rules/triggers.md` 전체를 읽지 않는다. 코드/요청과 관련된 키워드로 이 파일을 먼저 grep해서(예: `grep -n "^## " rules/triggers.md`로 47개 제목만 훑거나, 의심되는 키워드로 직접 grep) 몇 번 트리거에 해당하는지 후보를 추린 다음, 그 번호의 블록만 읽는다. **후보가 여러 개(2개 이상)면 번호마다 따로 grep하지 말고 알터네이션으로 한 번에 묶는다** — `grep -A 8 -E "^## (1|12|17|20|29|35|36|39|47)\." rules/triggers.md` 처럼. 트리거 하나씩 grep -A 20을 반복하면 도구 호출 수와 중복 컨텍스트가 그만큼 늘어난다(복잡한 시나리오에서 트리거 10개 이상 걸릴 때 이 차이가 크다 — [examples/token-comparison-test.md](examples/token-comparison-test.md) 후속 라운드 참고). 해당하는 게 없으면 아무 말도 하지 않는다. 대부분은 "코드에 이게 보이면" 걸리지만, 일부(1·16·35·36·37번)는 "있어야 할 게 안 보이면" 걸리는 부재형이다. 부재형은 애초에 코드에 특정 키워드가 없으므로, 키워드 grep보다 47개 제목 목록을 먼저 훑어 해당 여부를 판단하는 쪽이 낫다. 트리거는 세 유형이다: **부재형**(있어야 할 게 없어서 걸림, grep 같은 존재-탐지로는 못 잡음 — 예: 나이확인 로직 자체가 없음), **존재형-단순**(코드에 패턴이 있으면 바로 걸림, 오탐 위험 낮음), **존재형-문맥확인**(패턴이 있어도 진짜 문제인지 한 번 더 확인 — 예: RAG 검색 vs 사용자 대화 학습재사용, SNS 팔로우 vs 유료 구독, 테스트/학습용 키 vs 실제 운영 키).
2. 아래 "판단 원칙"을 따른다.
3. `workflows/trigger-to-verification.md`에 정의된 절차로 실시간 조회한다: **law.go.kr 직접 조회(Browser 도구로 렌더링, 기본 경로)** → (설치돼 있으면) beopsuny 스킬 → korean-law-mcp(OC 코드 있는 예외적 경우만) → 법망 API(비공식, 최후 수단). 단, `rules/triggers.md`에 오늘 날짜로 `[VERIFIED]`된 기록이 이미 있으면 재조회 없이 그 기록을 인용한다 — law.go.kr 왕복이 비용의 대부분을 차지하므로([examples/token-comparison-test.md](examples/token-comparison-test.md)) 같은 날 같은 조문을 반복 조회하지 않는다.
4. 아래 "라벨"에 따라 `[VERIFIED]/[UNVERIFIED]/[INSUFFICIENT]/[REGULATORY-GAP]`을 붙인다.
5. 아래 "출력 포맷"대로 짧게 출력한다.

## 판단 원칙

1. **기억으로 답하지 않는다.** 조문 번호·벌칙 금액·시행일을 학습된 기억에서 꺼내 쓰지 않는다. 조회 없이 나온 조문·금액은 결론에 못 쓴다(단, 위 3번의 당일 재사용 예외는 적용).
2. **트리거 ≠ 결론.** "이 패턴이면 이 법이 관련될 수 있다"는 후보일 뿐, 그 자체가 위반/괜찮음의 결론이 아니다. "확인이 필요하다"까지만 말한다.
3. **불확실성이 기본값이다.** 확인 못 한 건 못 했다고 말한다. "아마도"·"일반적으로" 같은 표현으로 근거를 흐리지 않는다.
4. **질문을 기다리지 않는다.** "법적으로 괜찮아?"라고 안 물어봐도 트리거가 보이면 먼저 말한다. 매번 장황한 자문처럼 굴지 않고, 짧게 짚고 필요하면 나중에 깊게 들어간다.
5. **확신의 강도는 검증 범위를 넘지 않는다.** `[VERIFIED]`만 조문을 인용한다. `[UNVERIFIED]`는 "~로 보인다" 이상 단정하지 않는다. `[INSUFFICIENT]`는 결론 없이 어디를 더 봐야 하는지만 안내한다.
6. **변호사가 아니다.** "확인해봐야 한다"까지 하고 "이러면 100% 안전하다"는 보장을 하지 않는다. 실제 처리(계약, 신고, 등록)는 본인이나 전문가에게 미룬다.
7. **사실관계가 결론을 가르면, 그 사실관계를 되묻는다.** "표면적으로 보이는 사실"과 "법적으로 중요한 실질"이 다른 경우가 많다(GPS를 안 쓴다고 위치정보가 아닌 게 아니고, 최종 결정을 사람이 한다고 모든 대상자가 사람 검토를 받는 게 아니다). 결론이 구체적 구현 방식(자동/수동, 누가 실제로 하는지)에 따라 갈리면 단정하지 말고 확인부터 한다.

배경과 실제 발견 사례는 `rules/legal-reasoning.md`(전체 원문), `examples/`(재현 테스트·할루시네이션 테스트·애매한 질문 테스트) 참고 — 걸릴 때마다 매번 열어볼 필요는 없고, 원칙이 왜 이렇게 정해졌는지 궁금할 때만 보면 된다.

## 라벨

| 라벨 | 조건 |
|---|---|
| `[VERIFIED]` | law.go.kr/korean-law-mcp/beopsuny 원문을 직접 대조했다. 조문 번호만으로 부족 — 최근 개정(신설된 항·호) 유무까지 확인 |
| `[UNVERIFIED]` | 트리거는 걸렸지만 원문 대조를 못했다 (기본값) |
| `[INSUFFICIENT]` | 조회 자체가 실패했다(law.go.kr 접속 실패, 조문 특정 실패 등) |
| `[REGULATORY-GAP]` | 위반 여부가 아니라 "이 구조를 규율하는 조문이 아직 없다"는 걸 원문으로 확인했다(트리거 47번 참고). "규제 없음" ≠ "안전함" — 언제든 신설될 수 있다는 점을 덧붙인다 |

세부 조건·예시는 `rules/uncertainty.md` 참고.

## 출력 포맷

```
🔍 LawLint: [트리거 번호·이름]

확인 대상: {부재형은 "무엇이 있는데 무엇이 없는지" 한 줄, 존재형은 구체적 파일:라인/함수}
관련 가능성: {법령명 + 조문(있으면)}
상태: [VERIFIED] / [UNVERIFIED] / [INSUFFICIENT] / [REGULATORY-GAP]
확인 경로: {law.go.kr 원문 확인 / korean-law-mcp / 법망 API / 조회 실패}

확인이 필요한 것: {한 줄}
참고 링크: {있으면}

⚠️ 이건 법률 자문이 아니라 확인 지점 안내입니다. 실제 처리는 직접 확인하거나 전문가 자문을 받으세요.
```

트리거 하나당 이 블록 하나. 여러 개 걸리면 우선순위 높은 것(개인정보처리방침 미게시처럼 전면적인 것)부터. `[INSUFFICIENT]`면 "확인이 필요한 것" 대신 "왜 실패했는지"와 "어디서 직접 확인하면 되는지"를 적는다. 같은 세션에서 이미 안내한 트리거는 코드가 안 바뀌었으면 반복하지 않고, 개발자가 "괜찮다고 확인했다"고 하면 그 세션 동안 다시 꺼내지 않는다.

형식·규칙의 배경은 `templates/legal-review-report.md` 참고.

## v0.1의 알려진 한계

- **law.go.kr 직접 조회가 기본 경로로 확정됨(2026-09-17)** — korean-law-mcp(제3자 중계 서버, OC 코드 필요)는 기본 경로에서 제외. 단, law.go.kr은 자바스크립트 렌더링 사이트라 일반 WebFetch로는 빈 껍데기만 나오고, **Browser 도구(`navigate`+`get_page_text`)로 열어야** 조문 전문이 나온다는 걸 실측으로 확인했다 — `workflows/trigger-to-verification.md` 참고
- 법망 API는 2026-09-17 기준 503으로 응답 없음 (비공식 서버, 가용성 보장 안 됨) — 최후 수단으로만 사용
- `rules/triggers.md`의 47개 항목 중 일부(28·30·31·33·34·44·46번)는 아직 원문 대조 전(`[UNVERIFIED]`/`[INSUFFICIENT]`) 상태 — 이 스킬이 실제로 걸릴 때마다 그 순간 다시 조회해야 하며, triggers.md에 적힌 조문을 그대로 믿고 인용하지 않는다. 2026-09-17 재검증 라운드에서 나머지 항목(1·2·3·6·17·24·25·26·27·29·32·35·36·37·38·39·40·41·42·43·45번)은 law.go.kr 원문으로 직접 대조 완료 — 이 과정에서 실제 오류 6건(36번 휴면계정 규정 폐지, 25번 규율 대상 오인, 43번 근거 조문 과장, 39번 시행 전 개정사항, 38번 미해결 쟁점 일부 해소, 6번 고영향 AI 의무에 실제로는 직접 과태료가 없다는 점)을 찾아 정정함
- **형사처벌 소지가 있는 27번(비대면진료)을 최우선으로 재검증**: 지금(2026-09-17) 비대면진료 앱의 적법성은 "감염병 위기경보가 심각 단계인지"라는, 코드로 알 수 없는 실시간 행정 사실 하나에 달려있다는 걸 law.go.kr로 확인함(의료법 제17조·제34조의2, 감염병예방법 제49조의3) — 이 사실을 확인하지 않고 합법/불법을 단정하면 안 됨(원칙 7). 같은 라운드에서 인공지능기본법(2026-01-22 시행) 원문도 처음 확인해 트리거 2·3·6에 실제 조문·과태료를 채워 넣었다
- 2026-09-17 재현율 자가점검(웹검색 없이 triggers.md의 코드 패턴과 이 description 키워드만 대조) 결과, 46개 중 17개가 description에 키워드가 없어 실제로는 감지되지 않을 가능성이 높았다 — 발견 즉시 description에 반영 완료. 새 트리거를 추가할 때는 반드시 이 description도 같이 갱신할 것 (누락되면 SKILL.md가 절대 발동하지 않는다)
- 같은 날 실제 오픈소스 저장소 6개로 재현율 테스트도 진행함([examples/](../examples/) repro-test-*.md 6개 파일). 1번(나이 확인)은 6개 중 6개 모두 미흡하거나 부재 — 부재형 트리거가 실무에서 가장 자주 쓰일 규칙으로 확인됨. 47번(매칭형 서비스 규제 공백)처럼 "위반 여부"가 아니라 "규제가 아직 없다"는 걸 안내해야 하는 특수 케이스도 발견됨 — `rules/uncertainty.md`에 4번째 라벨 `[REGULATORY-GAP]`을 추가해 반영함
- 할루시네이션 테스트([examples/hallucination-test.md](../examples/hallucination-test.md))에서 검증 없이 답하면 구체적 숫자(과태료·과징금 비율)를 틀리거나 최근 개정을 놓친다는 게 실제로 확인됨. 애매한 질문 테스트([examples/ambiguous-question-test.md](../examples/ambiguous-question-test.md))에서는 사실관계(자동/수동 여부 등)가 결론을 가르는 상황에서 단정하지 않고 되묻는 게 중요하다는 게 확인돼 `rules/legal-reasoning.md` 원칙 7번으로 반영됨. 엔드투엔드 드라이런([examples/dryrun-board.md](../examples/dryrun-board.md)) 중에는 1번 트리거 자체가 조문 번호 없이 방치돼 있던 걸 발견해 정정함 — 부품 단위 테스트로는 못 잡고 전체를 이어봐야 잡히는 종류의 문제였음
- **토큰 사용량(2026-09-19 실측, 3차)**: `description` 필드(2,052 bytes)는 스킬이 걸리든 안 걸리든 세션마다 항상 로드된다. `rules/triggers.md` 전체를 읽는 대신 헤더만 grep 후 해당 블록만 읽으면 그 파일 하나를 읽는 비용만 따졌을 때 크게 절감되지만, **스킬 전체를 켰을 때와 안 켰을 때를 직접 비교하면 이야기가 다르다**([examples/token-comparison-test.md](../examples/token-comparison-test.md)). 단순 시나리오(트리거 1~2개)에서는 베이스라인보다 **26~37% 더** 쓴다. 트리거가 10개 이상 겹치는 복잡한 시나리오에서는 격차가 **+67%까지** 벌어진 게 실측됐는데, 원인은 트리거 번호 하나하나 개별 `grep -A`를 반복 호출한 것이었다 — SKILL.md 절차 1번에 "후보가 여럿이면 알터네이션으로 한 번에 묶어라"는 지침을 추가하고 트리거 항목의 중복 서술을 정리한 뒤 재측정하니 같은 시나리오에서 **+24%로 줄었다**(도구 호출 9회→3회, 걸리는 트리거 수는 오히려 11개→12개로 유지). 즉 시나리오가 복잡할수록 격차가 커지는 경향 자체는 여전하지만, grep 호출 방식이 그 격차의 상당 부분을 차지했다는 게 확인됨. "LawLint가 토큰을 아껴준다"고 말하면 안 된다 — 여전히 베이스라인보다 더 쓴다.

## 하지 않는 것

- 확정적 법률 자문, 소송 결과 예측
- 계약 체결·신고·등록 등 실제 법적 효과가 있는 행동의 대행
- 조회 없이 조문·금액을 만들어내는 것
